syntax = "proto3";

import "Value.proto";

option java_package = "cn.zhang.grpc";

service Calculator {

    rpc calculator (Value) returns (Value) {}

    rpc calculatorServerStream (Value) returns (stream Value) {}
}